package dev.the456gamer.gamearena.tictactoe.player;

/**
 * A player that is controlled by a human.
 */
public class HumanPlayer {
    // needs to handle clicking on tiles -> gets active player -> if instance of human player -> make move (location)
    // needs to support waiting for that event handler for long periods of time


}
